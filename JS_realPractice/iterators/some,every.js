const numbers = [1, 2, 3, 4, 5]
const someResult = numbers.some((item) => item % 2 === 0)
const everyResult = numbers.every((item) => item % 2 === 0)
console.log(someResult)
console.log(everyResult)
