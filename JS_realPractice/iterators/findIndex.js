const words = ["shaik", "Musharaf", "Mohammad", "Paryaz"]
const result = words.findIndex((item) => item.length > 6)
console.log(result)
//it returns only the first element index satisfying the mentioned condition
